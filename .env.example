# Copie para .env e ajuste se necessário. Todas as variáveis já possuem
# valores padrão no docker-compose.yml, então .env é opcional para rodar
# localmente.

POSTGRES_USER=lotiado
POSTGRES_PASSWORD=lotiado
POSTGRES_DB=lotiado
POSTGRES_PORT=5432

REDIS_PORT=6379

OLLAMA_PORT=11434

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET=lotiado
